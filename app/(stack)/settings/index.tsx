import { View, Text } from 'react-native';

const SettingsScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="font-work-black text-3xl">Ajustes</Text>
    </View>
  );
};

export default SettingsScreen;
