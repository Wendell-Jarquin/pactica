import { View, Text } from 'react-native';

const ProfileScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="font-work-black text-3xl">Perfil</Text>
    </View>
  );
};

export default ProfileScreen;
